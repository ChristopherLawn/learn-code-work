{{!-- each comment will display user email above the comment --}}
<div class="comments">
  {{#each this}}

  <section class="comment">
    <div class="meta">
      {{user.email}}
    </div>

    <div class="text">
      {{comment_text}}
    </div>
  </section>

  {{/each}}
</div>